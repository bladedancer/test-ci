{
	"devDependencies": {
		"commander": "^2.16.0",
		"lerna": "^2.11.0",
		"npm": "^6.2.0",
		"simple-git": "^1.96.0"
	},
	"scripts": {
		"bootstrap": "npm install && lerna bootstrap --hoist",
		"build": "lerna run build",
		"test": "lerna run test"
	}
}
